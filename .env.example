# Base application URL. For local development this might be http://localhost:3000
APP_URL=https://isntx.com

# Stripe secret API key used by the server to create sessions and retrieve data
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe webhook signing secret. This verifies that webhook requests
# genuinely originate from Stripe.
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
